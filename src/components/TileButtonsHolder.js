import styled from 'styled-components';
import theme from '../theme';

const TileButtonsHolder = styled.div`
  display: flex;
  justify-content: flex-start;
  margin: 8px;
`;

export default TileButtonsHolder;
