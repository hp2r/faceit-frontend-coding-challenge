import styled from 'styled-components';
import theme from '../theme';

const TournamentTitle = styled.h6`
  ${theme.typography.h6};
  margin: 14px;
  margin-bottom: ${theme.spacing(4)};
`;

export default TournamentTitle;
